<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css"
            rel="stylesheet"
            integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx"
            crossorigin="anonymous"
        />
        <title>Register</title>
    </head>

    <body>
        <%- include("partials/navbar") %>

        <div class="container">
            <div class="row mt-5 align-content-center justify-content-center">
                <div class="col-sm-8 col-md-6 col-lg-4 shadow rounded-4 p-4">
                    <form
                        id="registerForm"
                        action="/users/register"
                        method="POST"
                    >
                        <h1 class="display-5">Register user</h1>

                        <div class="form-group has-validation mb-3">
                            <label for="username" class="form-label"
                                >Username</label
                            >
                            <input
                                name="user"
                                class="form-control"
                                id="username"
                                required
                            />
                        </div>

                        <div class="mb-3">
                            <label for="password" class="form-label"
                                >Password</label
                            >
                            <input
                                type="password"
                                name="pass"
                                class="form-control"
                                id="password"
                                required
                            />
                        </div>

                        <div class="mb-3">
                            <button
                                type="submit"
                                class="btn btn-primary w-25"
                                id="register"
                            >
                                Register
                            </button>
                            <button
                                type="button"
                                onclick="location.href='/login'"
                                class="btn btn-secondary w-25"
                            >
                                Login
                            </button>
                        </div>

                        <div class="mb-0" style="text-align: center">
                            <% if (message.length > 0) { %> <% if
                            (message[0]==="Success") { %>
                            <p class="alert alert-success mb-0" role="alert">
                                <%= message[0] %>
                            </p>
                            <% } else { %>
                            <p class="alert alert-danger mb-0" role="alert">
                                <%= message[0] %>
                            </p>
                            <% } %> <% } %>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <script
            src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa"
            crossorigin="anonymous"
        ></script>
        <script src="/backend/views/apiControl.js"></script>
    </body>
</html>
